<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:prism="http://prismlibrary.com"
                 xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup" 
                 xmlns:Icons="clr-namespace:Plugin.Iconize;assembly=Plugin.Iconize"
                 xmlns:pkv="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
                 xmlns:i18n="clr-namespace:Resx;assembly=Resx" 
                 xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
                 prism:ViewModelLocator.AutowireViewModel="True"
                 x:Class="Covid_Info.Views.ExternalInfo"
                 Visual="Material">
    <pages:PopupPage.Animation>
        <animations:ScaleAnimation 
            PositionIn="Center"
            PositionOut="Bottom"
            ScaleIn="1.2"
            ScaleOut="0.8"
            DurationIn="400"
            DurationOut="300"
            EasingIn="SinOut"
            EasingOut="SinIn"
            HasBackgroundAnimation="True"/>
    </pages:PopupPage.Animation>
    
    <pkv:PancakeView  BackgroundColor="{StaticResource BackgroundColorPage}"  CornerRadius="10,10,0,0"  HasShadow="True" Margin="0,10,0,0" >
        <pkv:PancakeView.GestureRecognizers>
            <SwipeGestureRecognizer Direction="Down" />
        </pkv:PancakeView.GestureRecognizers>
        
        <StackLayout>

            <pkv:PancakeView Padding="10" HasShadow="True" >
                <pkv:PancakeView BackgroundColor="{StaticResource BaseFontColor}" HeightRequest="5" WidthRequest="70" HorizontalOptions="Center" CornerRadius="20"/>
                <pkv:PancakeView.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding CloseCountryDetails}"/>
                </pkv:PancakeView.GestureRecognizers>
            </pkv:PancakeView>
            <pkv:PancakeView BackgroundColor="red" Padding="5">

                <Label Text="{i18n:Translate officialInformation}" 
                           FontSize="Large"
                           FontAttributes="Bold"
                           TextColor="{StaticResource WhiteColor}"
                           HorizontalOptions="Center"/>
            </pkv:PancakeView>
            <ScrollView Margin="15">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="5"/>
                    </Grid.RowDefinitions>
                    <!--ELSALVADOR INFO-->
                    <pkv:PancakeView Grid.Row="0" BackgroundColor="{StaticResource WhiteColor}"  IsVisible="{Binding IsVisibleGOES}" CornerRadius="4" Elevation="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <pkv:PancakeView Grid.Column="0" >
                                <Image Grid.Column="0" Source="sv" WidthRequest="75" HeightRequest="75" BackgroundColor="{StaticResource CustomNavBarColor}"/>
                            </pkv:PancakeView>
                            <Label Grid.Column="1"
                               Text="{i18n:Translate goesInfo}"
                               FontSize="Body"
                               TextColor="{StaticResource BaseFontColor}"
                               Padding="5"
                                   VerticalTextAlignment="Center"/>
                        </Grid>
                        <pkv:PancakeView.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding goSVCovidInfo}"/>
                        </pkv:PancakeView.GestureRecognizers>
                    </pkv:PancakeView>

                    <!--Q&A-->
                    <pkv:PancakeView Grid.Row="1" BackgroundColor="{StaticResource WhiteColor}" CornerRadius="4" Elevation="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="question" WidthRequest="75" HeightRequest="75"/>
                            <Label Grid.Column="1"
                               Text="Preguntas y respuestas sobre la enfermedad por coronavirus (COVID-19)"
                               FontSize="Body"
                               TextColor="{StaticResource BaseFontColor}"
                               Padding="5"/>
                        </Grid>
                        <pkv:PancakeView.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding goWHOCovidFaqs}"/>
                        </pkv:PancakeView.GestureRecognizers>
                    </pkv:PancakeView>

                    <!--WHO DONATIONS-->
                    <pkv:PancakeView Grid.Row="2" BackgroundColor="{StaticResource WhiteColor}" CornerRadius="4" Elevation="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="donate" WidthRequest="75" HeightRequest="75"/>
                            <Label Grid.Column="1"
                               Text="Donar al fondo de Respuesta a la COVID-19"
                               FontSize="Body"
                               TextColor="{StaticResource BaseFontColor}"
                               Padding="5"/>
                        </Grid>
                        <pkv:PancakeView.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding goWHODonations}"/>
                        </pkv:PancakeView.GestureRecognizers>
                    </pkv:PancakeView>

                    <!--WHO COVID MYTHS-->
                    <pkv:PancakeView Grid.Row="3" BackgroundColor="{StaticResource WhiteColor}" CornerRadius="4" Elevation="15">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Source="fake" WidthRequest="75" HeightRequest="75" BackgroundColor="Black" Margin="0"/>
                            <Label Grid.Column="1"
                               Text="Consejos para la población acerca de los rumores sobre el nuevo coronavirus"
                               FontSize="Body"
                               TextColor="{StaticResource BaseFontColor}"
                               Padding="5"/>
                        </Grid>
                        <pkv:PancakeView.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding goCovidWHOMyths}"/>
                        </pkv:PancakeView.GestureRecognizers>
                    </pkv:PancakeView>
                </Grid>
            </ScrollView>
            
        </StackLayout>

    </pkv:PancakeView>

</pages:PopupPage>