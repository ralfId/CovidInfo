<?xml version="1.0" encoding="utf-8" ?>
<views:TemplatePage
             xmlns:views="clr-namespace:Covid_Info.Views"
             xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             xmlns:contentViews="clr-namespace:Covid_Info.Views.ContentViews"
             xmlns:Icons="clr-namespace:Plugin.Iconize;assembly=Plugin.Iconize"
             xmlns:i18n="clr-namespace:Resx;assembly=Resx" 
             xmlns:lottie="clr-namespace:Lottie.Forms;assembly=Lottie.Forms"
             xmlns:xamanimation="clr-namespace:Xamanimation;assembly=Xamanimation"
             xmlns:pkv="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
             x:Class="Covid_Info.Views.MainPage"
             Visual="Material"
             Title="{Binding Title}">

    <views:TemplatePage.Resources>
        <ResourceDictionary>
            <xamanimation:ScaleToAnimation
                x:Key="ScaleToAnimation"
                Target="{x:Reference attentionJSON}"
                Scale="2"/>
        </ResourceDictionary>
    </views:TemplatePage.Resources>
    <AbsoluteLayout>
        
        <StackLayout AbsoluteLayout.LayoutBounds="0,0,1,1"
                        AbsoluteLayout.LayoutFlags="All"
                        IsVisible="{Binding IsVisibleMainPage}" Spacing="0">
            <!--header-->
            <pkv:PancakeView BackgroundColor="#0026ca" CornerRadius="0,0,10,10" Margin="0" Padding="5,5" >
                <!--<Label TextColor="{StaticResource WhiteColor}"
                       FontAttributes="Bold"
                       FontSize="Body"
                           HorizontalOptions="Center" IsVisible="False">
                    <Label.FormattedText>
                        <FormattedString>
                            <Span Text="{i18n:Translate lastUpdta}"/>
                            <Span Text="{Binding LastUpdateDateTime}" />
                        </FormattedString>
                    </Label.FormattedText>
                </Label>-->
            </pkv:PancakeView>
            
            <ScrollView Margin="0" Padding="0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                        <RowDefinition Height="auto"/>
                    </Grid.RowDefinitions>

                    <!--Notification panel-->
                    <!--<contentViews:UpdaterViewer Grid.Row="1" IsVisible="{Binding IsVisibleUpdaterViwer}"/>-->

                    <!--current country info-->
                    <contentViews:MyCountry Grid.Row="2" Margin="10,0" IsVisible="{Binding IsVisibleMyCountry}"/>
                    <!--global info-->
                    <contentViews:GlobalInfo  Margin="10,0" Grid.Row="3"/>
                    

                    <Frame Grid.Row="4"   Padding="0" CornerRadius="2" Margin="10,0">
                        <Grid Margin="10,5" ColumnSpacing="0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="auto"/>
                            </Grid.ColumnDefinitions>

                            <lottie:AnimationView
                            Grid.Column="0"
                            WidthRequest="70" HeightRequest="70" 
                            Margin="0"
                            x:Name="LottieView" 
                            Animation="globe-rotation.json" 
                            Loop="True" 
                            AutoPlay="True"
                            VerticalOptions="FillAndExpand"
                            HorizontalOptions="FillAndExpand" />

                            <Label Grid.Column="1" Text="{i18n:Translate countriesStats}"
                        FontAttributes="Bold"
                           FontSize="Body"
                        TextColor="{StaticResource BaseFontColor}"
                           HorizontalOptions="CenterAndExpand"
                               VerticalOptions="Center"/>
                            <Icons:IconLabel Grid.Column="2"
                               Text="fas-arrow-circle-right"
                               FontSize="Large"
                               FontAttributes="Bold"
                               TextColor="{StaticResource BaseFontColor}"
                               HorizontalOptions="End"
                                         VerticalOptions="Center"/>
                        </Grid>
                        <Frame.GestureRecognizers>
                            <TapGestureRecognizer Command="{Binding navAllCountries}"/>
                        </Frame.GestureRecognizers>
                    </Frame>

                    <!--most affeccted countries-->
                    <contentViews:MostAffectedCountries Grid.Row="5" IsVisible="{Binding IsVisibleMAC}" Margin="10,0,0,30"/>
                    <!--external info-->
                    <!--<contentViews:ExternalInfo Grid.Row="6" />-->

                    
                </Grid>
            </ScrollView>

            <Frame Padding="10,5" Margin="-5" HeightRequest="125" BackgroundColor="#eeeeee" >
                <Grid HorizontalOptions="Center" ColumnSpacing="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="0.15*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    

                    <Icons:IconLabel x:Name="attentionJSON"
                            Grid.Column="0"
                               Text="fas-angle-up"
                               FontSize="Title"
                               FontAttributes="Bold"
                               TextColor="{StaticResource BaseFontColor}" VerticalOptions="Center" HorizontalOptions="Center" Margin="0"/>
                    <Label Grid.Column="1" Text="Presiona para conocer mas de COVID19" FontSize="Body" VerticalOptions="Center" TextColor="{StaticResource BaseFontColor}"/>
                </Grid>
                <Frame.GestureRecognizers>
                    <TapGestureRecognizer Command="{Binding goExternalInfo}"/>
                </Frame.GestureRecognizers>
            </Frame>


        </StackLayout>

        <contentViews:LoadingData  AbsoluteLayout.LayoutBounds=".5,.5,.5,.5"
                                       AbsoluteLayout.LayoutFlags="All"
                                       IsVisible="{Binding IsVisibleLoadingPage}"/>
    </AbsoluteLayout>
    

</views:TemplatePage>