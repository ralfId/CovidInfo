<?xml version="1.0" encoding="utf-8" ?>
<pages:PopupPage  xmlns="http://xamarin.com/schemas/2014/forms"
                  xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                  xmlns:d="http://xamarin.com/schemas/2014/forms/design"
                  xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                  xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup" 
                  mc:Ignorable="d"
                  x:Class="Covid_Info.Views.Popups.CountrySorting">
    <pages:PopupPage.Content>
        <Grid RowDefinitions="auto,auto,auto,auto,auto">
            <Button Text="ascending"
                         Command="{Binding AscendingSortingCommand}"/>
            <Button Grid.Row="1" Text="descending"
                         Command="{Binding DescendingSortingCommand}"/>
            <Button Grid.Row="2" Text="Least affected countries"
                         Command="{Binding LeastAffectedSortingCommand}"/>
            <Button Grid.Row="3" Text="Most affected countries"
                         Command="{Binding MostAffectedSortingCommand}"/>
            <Picker />
        </Grid>
    </pages:PopupPage.Content>

</pages:PopupPage>